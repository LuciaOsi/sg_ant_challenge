import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    height: '100%',
    padding: 16,
    justifyContent: 'space-around',
  },
  welcomeText: {
    fontSize: 25,
    marginTop: '15%',
    textAlign: 'center',
  },
});
